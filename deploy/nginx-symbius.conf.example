# Exemplo legado (nginx). Nesta VPS usamos Traefik — ver deploy/stack.yml.
# Mantido só como referência.

upstream symbius_central {
    server 127.0.0.1:5010;
    keepalive 64;
}

server {
    listen 80;
    server_name flow.symbius.com.br central.symbius.com.br;

    location / {
        proxy_pass http://symbius_central;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
    }
}
